#include <iostream>
using namespace std;

int main()
{
    int age;
    char studentID;
    
    cout << "Please enter your age: " << endl;
    cin >> age;
    cout << "Do you have a student ID? (Y/N): " << endl;
    cin >> studentID;

    if (studentID == 'Y'){

            }
            else if (studentID == 'N'){

            }
            else {
                cout << "Invalid characters. Please type Y/N only.";
            }

    switch(age)
        case 1: case 2:
            if (age >= 2 && age <= 3)
                
        break;


    return 0;
}